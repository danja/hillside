{
  "name": "hillside-media-visualizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/lib && cp src/lib/*.js dist/lib/",
    "preview": "vite preview",
    "test": "vitest run",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "^2.0.5",
    "jsdom": "^26.1.0",
    "vite": "^5.4.1",
    "vitest": "^2.0.5"
  }
}
